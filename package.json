{
  "name": "physio-fable",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "fable": "fable Client.fsproj --outDir build",
    "render": "node scripts/render.mjs",
    "build": "npm run fable && npm run render",
    "clean": "rm -rf build dist"
  },
  "dependencies": {
    "fable-library": "*"
  }
}
